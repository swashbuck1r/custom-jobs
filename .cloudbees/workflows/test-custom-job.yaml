apiVersion: automation.cloudbees.io/v1alpha1
kind: workflow
name: My workflow
on:
  workflow_dispatch:
jobs:
  build:
    steps:
      - name: Say hello
        uses: docker://golang:1.20.3-alpine3.17
        shell: sh
        run: |
          echo "hello world"
    needs:
      - custom-approval
  custom-approval:
    timeout-minutes: 4320
    delegates: swashbuck1r/custom-jobs/custom-job.yml@main
    with:
      approvers: 310969a0-e7fb-11ea-a794-42010a83ae1a
      disallowLaunchByUser: false
      instructions: ""
      approvalInputs: |
        {}
